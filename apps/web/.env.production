# Production build overrides local development values.
PUBLIC_API_URL=https://api.funsona.com/api
PUBLIC_SITE_URL=https://funsona.com

# TODO: replace with a real Clerk PRODUCTION publishable key (pk_live_...)
# before deploying — the test key used in development must not go live.
# CLERK_SECRET_KEY (private) must never be committed here: set it as a
# Cloudflare Pages environment variable / wrangler secret instead.
PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_REPLACE_ME

PUBLIC_GOOGLE_ANALYTICS_ID=G-Y988WVVQFH
PUBLIC_GOOGLE_ADSENSE_CLIENT=ca-pub-2600827684802809
